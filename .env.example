# La API de Pulse. Se lee en tiempo de compilación, así que cambiarla obliga a
# reconstruir. Si no se define, el receptor usa el despliegue de Render.
VITE_API_URL=https://pulse-api-mq9p.onrender.com/v1
